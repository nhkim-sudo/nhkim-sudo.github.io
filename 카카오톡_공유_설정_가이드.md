# 🔧 카카오톡 공유 기능 설정 가이드

## 📋 체크리스트

### ✅ 1단계: 카카오 디벨로퍼스 설정 확인

#### 1-1. 앱 생성 확인
- https://developers.kakao.com 접속
- 내 애플리케이션 → 앱 선택
- **앱 이름**: 아무거나 (예: 우리결혼식)

#### 1-2. JavaScript 키 복사
1. **앱 설정 → 요약 정보** 페이지로 이동
2. **JavaScript 키** 복사 (예: `abc123def456...`)
   - ⚠️ `Native 앱 키`가 아님!
   - ⚠️ `REST API 키`가 아님!
   - ✅ **JavaScript 키**만 사용!

```
형식 예시:
✅ v42a6372a0310382d901959e6dbb700  (앞에 v)
✅ 3abc6372a0310382d901959e6dbb700  (숫자로 시작)
❌ fv42a6372a0310382d901959e6dbb700 (앞에 f 붙으면 안 됨)
```

#### 1-3. 플랫폼 설정 (중요!)
1. **앱 설정 → 플랫폼** 메뉴로 이동
2. **Web 플랫폼 등록** 클릭
3. **사이트 도메인** 입력:
   ```
   https://nhkim-sudo.github.io
   ```
   - ⚠️ 끝에 `/` 붙이지 마세요!
   - ⚠️ `http://`가 아니라 `https://`

#### 1-4. 활성화 설정 ON
1. **제품 설정 → 카카오 로그인** 메뉴
2. **활성화 설정** → **ON** 으로 변경
3. **Redirect URI** 등록 (선택사항, 로그인 안 쓰면 생략 가능)

---

## 🔧 2단계: 코드 수정

### 수정할 부분 1: JavaScript 키 입력

```javascript
// ⚠️ 이 부분을 찾아서 수정하세요!
const KAKAO_API_KEY = 'v42a6372a0310382d901959e6dbb700'; // 실제 키로 교체!
```

**수정 방법:**
1. 카카오 디벨로퍼스에서 JavaScript 키 복사
2. 위 코드에서 `'v42a6...'` 부분을 복사한 키로 교체

예시:
```javascript
// 카카오에서 받은 키가 'abc123def456...' 라면
const KAKAO_API_KEY = 'abc123def456...';
```

### 수정할 부분 2: 이미지 URL

```javascript
imageUrl: 'https://raw.githubusercontent.com/nhkim-sudo/nhkim-sudo.github.io/main/assets/img/kakaotalk.png',
```

**이미지가 없다면:**
- 이미지 준비 후 GitHub에 업로드
- 경로: `assets/img/kakaotalk.png`
- 또는 임시로 다른 이미지 URL 사용

**이미지 없이 테스트하려면:**
```javascript
imageUrl: 'https://via.placeholder.com/800x400.png?text=Wedding+Invitation',
```

---

## 🧪 3단계: 테스트

### 로컬 테스트 (브라우저 콘솔 확인)

1. **F12** (개발자 도구) 열기
2. **Console** 탭 확인
3. 아래 메시지가 보여야 함:
   ```
   Kakao SDK 초기화: true
   ```

4. **카카오톡 공유** 버튼 클릭
5. 콘솔에 에러 메시지 확인

### 자주 보는 에러와 해결법

#### 에러 1: "SDK is not initialized"
```
해결: Kakao.init() 코드 확인
→ JavaScript 키가 정확한지 확인
```

#### 에러 2: "Failed to validate"
```
해결: 카카오 디벨로퍼스 → 플랫폼 설정
→ 사이트 도메인이 정확히 입력되었는지 확인
→ https://nhkim-sudo.github.io (끝에 / 없이!)
```

#### 에러 3: "Invalid app key"
```
해결: JavaScript 키 다시 복사
→ Native 앱 키나 REST API 키가 아닌지 확인
```

#### 에러 4: "이미지를 불러올 수 없습니다"
```
해결: imageUrl 확인
→ 이미지가 실제로 존재하는지 확인
→ URL을 브라우저에 직접 입력해서 이미지 보이는지 확인
```

---

## 📱 4단계: 실제 카카오톡 테스트

### PC에서 테스트
1. https://nhkim-sudo.github.io 접속
2. **카카오톡 공유** 버튼 클릭
3. QR 코드 또는 카카오톡 PC 버전으로 전송

### 모바일에서 테스트
1. 모바일에서 https://nhkim-sudo.github.io 접속
2. **카카오톡 공유** 버튼 클릭
3. 카카오톡 앱이 자동으로 열림
4. 친구/채팅방 선택 후 전송

### 공유된 메시지 확인사항
- ✅ 제목: "이연진 ♥ 김나현 결혼합니다"
- ✅ 설명: "2026년 11월 7일..."
- ✅ 이미지 표시됨
- ✅ "청첩장 보기" 버튼 있음
- ✅ 버튼 클릭 시 https://nhkim-sudo.github.io 로 이동

---

## 🛠️ 문제 해결 플로우

```
카카오톡 공유 버튼 클릭
    ↓
아무 반응 없음?
    ↓
F12 → Console 확인
    ↓
"SDK is not initialized"? → JavaScript 키 확인
"Failed to validate"? → 플랫폼 설정 확인
"Invalid app key"? → 키 종류 확인 (JavaScript 키 맞는지)
이미지 에러? → imageUrl 확인
    ↓
수정 후 다시 테스트
    ↓
성공! 🎉
```

---

## 📝 완벽한 설정 예시

### 카카오 디벨로퍼스 설정
```
앱 이름: 우리결혼식
플랫폼: Web
사이트 도메인: https://nhkim-sudo.github.io
활성화 설정: ON (카카오 로그인)
```

### 코드 설정
```javascript
const KAKAO_API_KEY = 'abc123def456...'; // 실제 JavaScript 키

Kakao.Share.sendDefault({
    objectType: 'feed',
    content: {
        title: '이연진 ♥ 김나현 결혼합니다',
        description: '2026년 11월 7일 토요일 오후 12시 20분\n신도림 웨딩시티 11층 그랜드볼룸',
        imageUrl: 'https://raw.githubusercontent.com/nhkim-sudo/nhkim-sudo.github.io/main/assets/img/kakaotalk.png',
        link: {
            mobileWebUrl: 'https://nhkim-sudo.github.io/',
            webUrl: 'https://nhkim-sudo.github.io/'
        }
    }
});
```

---

## 💡 추가 팁

### 팁 1: 이미지 최적화
```
권장 크기: 800x400 px
권장 포맷: JPG, PNG
최대 용량: 500KB 이하
비율: 2:1 (가로가 세로의 2배)
```

### 팁 2: 메시지 커스터마이징
```javascript
content: {
    title: '제목 (최대 4줄)',
    description: '설명 (최대 3줄)',
    imageUrl: '이미지 URL',
    // imageWidth: 800, // 선택사항
    // imageHeight: 400, // 선택사항
}
```

### 팁 3: 버튼 추가/수정
```javascript
buttons: [
    {
        title: '청첩장 보기',
        link: { ... }
    },
    {
        title: '장소 안내',  // 2번째 버튼 추가 가능
        link: {
            mobileWebUrl: 'https://naver.me/5Rh0OxaM',
            webUrl: 'https://naver.me/5Rh0OxaM'
        }
    }
]
```

---

## 🔍 디버깅 체크리스트

배포 전 마지막 확인:

- [ ] JavaScript 키가 정확히 입력되었나?
- [ ] 플랫폼에 `https://nhkim-sudo.github.io` 등록되었나?
- [ ] 카카오 로그인 활성화 설정이 ON인가?
- [ ] 이미지 URL이 제대로 작동하나?
- [ ] GitHub Pages가 배포되었나?
- [ ] F12 콘솔에 "Kakao SDK 초기화: true" 보이나?
- [ ] 실제 카카오톡으로 테스트해봤나?

---

## 🆘 그래도 안 될 때

### 확인할 점:
1. 브라우저 캐시 삭제 (Ctrl + Shift + Del)
2. 시크릿 모드로 테스트
3. 다른 브라우저로 테스트 (Chrome, Safari, Edge)
4. 모바일로 테스트
5. 카카오 디벨로퍼스 설정 처음부터 다시

### 스크린샷 보내주실 것:
1. F12 Console 화면
2. 카카오 디벨로퍼스 → 앱 설정 → 요약 정보
3. 카카오 디벨로퍼스 → 앱 설정 → 플랫폼
4. 에러 메시지가 있다면 그것도!

화이팅! 🚀
